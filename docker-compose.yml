services:
  handwrittennotes:
    image: ghcr.io/jp-tx/handwrittennotes:latest
    ports:
      - "8080:8080"
    environment:
      - APP_PASSWORD=notes123
      - DATA_PATH=/data
    volumes:
      - ./notes:/data
    restart: unless-stopped
